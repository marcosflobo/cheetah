'use strict';

describe('cheetahApp.burndowns module', function() {

  beforeEach(module('cheetahApp.burndowns'));

  describe('burndowns controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('BurndownsController');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});